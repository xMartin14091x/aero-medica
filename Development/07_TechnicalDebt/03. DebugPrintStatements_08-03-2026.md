# Technical Debt: Debug Print Statements in Player Controller

**Logged:** 08-03-2026
**Severity:** Low — cosmetic, no gameplay impact
**File:** `scripts/gameplay/player_controller.gd`

---

## Problem

Four `print()` calls were added during animation debugging:

- Line 46: `print("Player animations found: ", anims)`
- Line 66: `print("Using idle='%s', walk='%s'" % [...])`
- Line 73: `print("Root bone: '%s'" % root_bone_name)`
- Line 134: `print("Stripped root motion track: %s in '%s'" % [...])`

## Fix

Remove all four `print()` lines before release. They pollute the Godot console output but cause no functional issues.
